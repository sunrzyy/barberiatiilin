// Función para realizar una reserva
function reservar(corte) {
  // Al hacer clic en "Reservar", se almacena el tipo de corte que se ha seleccionado
  // y redirige al usuario a la página de reserva (cuestionario.html)
  alert(`¡Has seleccionado el corte: ${corte}! Ahora puedes completar tu reserva.`);
}
